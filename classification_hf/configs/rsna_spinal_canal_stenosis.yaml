model:
  model_name: "/home/ge.polymtl.ca/p123239/.cache/huggingface/hub/models--raidium--curia/snapshots/9657dc56276bc6c9503ef6f8d060879c8bee482f"
  subfolder: "spinal_canal_stenosis"
  num_classes: 3
  attention_cfg: null

data_dir: "/home/ge.polymtl.ca/p123239/data/patches_RSNA_raw_with_mask_scs"
val_split: 0.15
seed: 42

epochs: 25
batch_size: 64
learning_rate: 0.005          # scaled internally: lr * batch_size / 256
num_workers: 8
eval_steps: 30

use_feature_caching: false    # use full model with mask → model(pixel_values=…, mask=…)

output_dir: "/home/ge.polymtl.ca/p123239/SpineFoundation/outputs_cls/rsna_scs"
